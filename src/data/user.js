class User{
    constructor(login, email, firstName, lastName, birthYear, gender) {
      this.login=login;
      this.email=email;
      this.firstName=firstName;
      this.lastName=lastName;
      this.birthYear=birthYear;
      this.gender=gender;
    }
}


export default User;